version: '3.9'

services:
  api:
    build:
      context: .
    image: cloud-api:latest
    ports:
      - '${PORT:-3000}:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    restart: unless-stopped
